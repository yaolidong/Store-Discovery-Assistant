server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: Add a reverse proxy for API calls if you want to route them through Nginx
    # Make sure to uncomment this if you want to use /api/... paths in your frontend code
    # and have Nginx forward them to the backend container.
    # location /api/ {
    #    proxy_pass http://backend:5000/; # 'backend' must be the service name in docker-compose.yml
    #    proxy_set_header Host $host;
    #    proxy_set_header X-Real-IP $remote_addr;
    #    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #    proxy_set_header X-Forwarded-Proto $scheme;
    #
    #    # The following might be needed if your Flask app expects a specific URL prefix
    #    # that it's not getting when requests are proxied.
    #    # proxy_redirect off;
    # }
}
